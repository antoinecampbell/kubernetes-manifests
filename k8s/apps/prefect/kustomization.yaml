apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

helmCharts:
  - name: prefect-server
    repo: https://prefecthq.github.io/prefect-helm
    version: 2026.5.16021822
    valuesFile: values.yaml
    releaseName: prefect-server
    namespace: default
    includeCRDs: true

resources:
  - secrets.yaml
